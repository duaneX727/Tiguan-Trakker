service_key.json
.env
__pycache__/
cleaned_*.csv
# Ignore other projects to clean up the Source Control tab
lab-server/
# Ignore nested independent project directories
K10-Lab/
trakker-analytics-api/
Tiguan-Project/
# Ignore all CSV files
*.csv
*.zip

# To make absolutely sure Git completely ignores those nested directories and doesn't pull in random Python scripts from them, it is safest to put your explicit rules above the directory-ignore rules, or scope the whitelist strictly to your root directory
!/*.py